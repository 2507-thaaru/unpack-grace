# Base URL of the settlement pipeline backend (FastAPI).
# Copy this file to .env.local (git-ignored) and set your own value.
# The server-side proxy at /api/proxy/* forwards to `${SETTLEMENT_API_URL}/api/...`.
SETTLEMENT_API_URL=https://your-backend-url.example.com
